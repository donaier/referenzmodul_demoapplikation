%h1= t('.title')

= simple_form_for([:cms, :cms_reference_topic_category, @cms_reference_topic], :url => cms_reference_topic_category_reference_topic_path(@cms_reference_topic.reference_topic_category, @cms_reference_topic), :as => :reference_topic, :html => { :class => 'form-horizontal' } ) do |f|

  = render 'form', :f => f

= link_to t('cms.back'), cms_reference_topic_categories_path
